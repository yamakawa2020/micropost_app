<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
  <%#下記と等価%>
  <%# @users.each do |user| %>
    <%#= render user %> <%#勝手に"_user.html.erb"をさがす%>
  <%# end %>
  
</ul>

<%= will_paginate %>